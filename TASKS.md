# Tasks

Format rules (the orchestrator and `/add-task` both depend on these):
- One `###` block per task. The ID is `T` + a number and is never reused.
- `Status:` is exactly one of `todo` | `in-progress` | `done` | `blocked`.
- `Depends:` lists task IDs, or `—`.
- `Files:` lists the paths the task is expected to touch. The orchestrator uses this to decide which tasks can run in parallel.
- `Acceptance:` must be checkable, either by a command or by a clear manual step on the Mac/device.
- When you finish a task, set `Status: done` and add a `Notes:` line (1–3 lines: decisions, follow-ups).

---

## Phase 0 — Foundation

### T01 Scaffold the Expo TypeScript app
Status: done
Depends: —
Files: package.json, package-lock.json, app.json, tsconfig.json, babel.config.js, eslint/prettier config, jest config, app/_layout.tsx, app/index.tsx, src/
Details: Create a new Expo app (latest SDK) with expo-router and a dev client (`expo-dev-client`), iOS only, New Architecture on, TS `strict`, typed routes on, path aliases (`@/` → `src/`). Bundle id `com.ejf.diorama` (placeholder). URL scheme `diorama`, so `diorama://settings`-style deep links open routes for Simulator checks. iOS deployment target at least 16.0 via `expo-build-properties` (the MKMapConfiguration APIs need iOS 16); keep the SDK default if it's already higher. Set `orientation: "default"` in app.json, **not** `portrait`: with `portrait` iOS can never rotate to landscape. Portrait is locked at runtime in T03. **Install every planned dependency now**, with `npx expo install` so versions match the SDK, so that later parallel tasks never touch package.json: expo-router and its peers (react-native-screens, react-native-safe-area-context, expo-linking, expo-constants, expo-status-bar), @tanstack/react-query, zustand, react-native-mmkv (and its peers), react-native-reanimated (and its peers), react-native-gesture-handler, @react-native-community/slider, expo-symbols, expo-haptics, expo-blur, expo-keep-awake, expo-screen-orientation, expo-build-properties. Dev: jest-expo, @testing-library/react-native, ESLint + Prettier. Set up Jest (`jest-expo` preset) with one smoke test. Add scripts: `typecheck`, `lint`, `test`, `ios`. Create the folder layout from OVERVIEW.md: `app/_layout.tsx` is a bare `Stack` and `app/index.tsx` is a placeholder. Use non-interactive flags for every generator.
Acceptance: `npm run typecheck && npm run lint && npm test` pass. `npx expo prebuild -p ios` succeeds, and `npx expo run:ios` launches the placeholder in the Simulator.
Notes: Expo SDK 57 (RN 0.86.3, React 19.2.3, TS 6.0); iOS target 16.4 is the SDK default. `typecheck` regenerates typed routes first (`expo customize tsconfig.json`). @testing-library/react-native pinned to 13.3.x (expo-router's `renderRouter` breaks on v14); `jest.setup.ts` mocks worklets/nitro, MMKV uses its in-memory fallback. `.gitignore` anchors `/ios` so `modules/diorama-native/ios/` stays tracked. After a fresh install, the first `simctl openurl` shows an "Open in 'Diorama'?" alert: press its "Open" button via System Events AXPress.

### T02 Theme tokens and Apple-style UI primitives
Status: done
Depends: T01
Files: src/theme/, src/ui/, app/dev/ui.tsx
Details: Tokens use iOS system colors via `PlatformColor` (label, secondaryLabel, systemBackground, secondarySystemGroupedBackground, systemBlue, separator…), the iOS type ramp (largeTitle…caption2, Dynamic Type friendly), and spacing (4-pt grid). Primitives: `Screen`, `InsetGroupedSection`, `ListRow` (with SF Symbol + chevron), `SkeletonRow`, `PrimaryButton` (capsule, filled), `GlassButton` (expo-blur material), `Symbol` (wrapper for expo-symbols). No large-title header or search field primitives: the native stack header provides those (`headerLargeTitle`, `headerSearchBarOptions`). Each primitive gets its own file and stays under ~80 lines.
Acceptance: A dev-only route `app/dev/ui.tsx` (open with `diorama://dev/ui`; it redirects to `/` outside `__DEV__`) renders every primitive. Simulator screenshots look right in light and dark mode. Typecheck passes.
Notes: SF Symbol wrapper is `SymbolIcon`: a component named `Symbol` shadows the global the React Compiler calls (`Symbol.for`). `GlassButton`/`GlassSurface` use Liquid Glass (expo-glass-effect) on iOS 26 with an expo-blur fallback; `Text` primitive has Dynamic Type variants. 52-pt rows / 26-pt corners match iOS 26 Settings (pixel-checked). Follow-up being fixed: large title invisible on `dev/ui`, separator insets/thickness. NEEDS DEVICE CHECK — glass reacts to touch; Reduce Motion stops skeleton pulse.

### T03 Providers: navigation stack, query client, stores
Status: done
Depends: T01
Files: app/_layout.tsx, app/index.tsx, app/city/[cityId].tsx, app/view/[cityId].tsx, app/settings.tsx, src/providers/, src/screens/, src/features/settings/store.ts, src/features/cities/recentsStore.ts
Details: The root `Stack` in `app/_layout.tsx` is wrapped in providers from `src/providers/`: `GestureHandlerRootView`, and a `QueryClientProvider` (staleTime 5 min). Routes: `app/index.tsx` → CityPicker, `app/city/[cityId].tsx` → CityPreview, `app/view/[cityId].tsx` → Viewer (presentation `fullScreenModal`, no header), `app/settings.tsx` → Settings. Each route file only renders its screen from `src/screens/<Name>/`; use placeholder screens for now. Native stack options: large title on the picker, `orientation: 'portrait'` by default, and landscape only on the Viewer. Zustand stores persisted with `react-native-mmkv`: settings (eyeSeparation 1.0, trackingSensitivity 1.0, miniatureIntensity 0.6, mode 'stereo', debugLook false) and recents (max 8).
Acceptance: `diorama://settings`, `diorama://city/paris` and `diorama://view/paris` each open their placeholder in the Simulator, and swipe-back works. A Jest test shows the settings store persists and rehydrates. `npm run typecheck && npm run lint && npm test` pass.
Notes: Deep links get the picker underneath (`unstable_settings.initialRouteName`). Viewer: `fullScreenModal`, landscape, `autoHideHomeIndicator`; hiding the status bar is left to T12 (red box while `UIViewControllerBasedStatusBarAppearance=false`). Settings store: `useSettings`, `useSetting`, setters clamp to `SETTING_RANGES` (trackingSensitivity 0.5–2, for T13 to confirm), `resetSettings`. Deep links, rotation, Done and swipe-back all checked in the Simulator. Follow-up being fixed: hairline under nav bar at rest; picker large title collapses after the Viewer closes when opened by deep link.

## Phase 1 — Native module (Swift)

### T04 Expo Module skeleton + mono DioramaMapView
Status: done
Depends: T01
Files: modules/diorama-native/**, app/dev/map.tsx
Details: Scaffold with `npx create-expo-module --local diorama-native`. It's interactive: if flags can't skip the prompts, write the module files by hand (expo-module.config.json, podspec, Swift, TS index). Add a native view `DioramaMapView` that wraps one `MKMapView` using `MKImageryMapConfiguration(elevationStyle: .realistic)`, with no POIs, no compass or scale, and interaction off. Props: center, altitude, pitch, heading, orbit. Apply them through `MKMapCamera`. Emit `onReady({ flyoverAvailable })` when the first full render finishes (`mapViewDidFinishRenderingMap` with fullyRendered). Infer `flyoverAvailable` from the 3D camera actually being accepted, or from a curated list if that's unreliable. Write a typed TS wrapper and a `ref` with `recenter()`. Comment the Swift for a React developer. The dev-only route `app/dev/map.tsx` (redirects to `/` outside `__DEV__`) reads optional `lat`, `lon`, `altitude`, `pitch`, `heading` search params and defaults to Manhattan.
Acceptance: A Simulator build compiles. `diorama://dev/map` shows Manhattan in 3D photoreal at altitude 1200 m, pitch 60° (check the screenshot). Changing props from JS moves the camera. `orbit` rotates slowly.
Notes: NEEDS DEVICE CHECK — orbit smoothness and heat on `diorama://dev/map?orbit=1`. MapKit gives no reliable Flyover signal, so `flyoverAvailable` comes from `modules/diorama-native/src/flyoverCoverage.ts` (61 cities checked by screenshot; Dubai and Mexico City are flat only). `altitude` = camera-to-center distance (`centerCoordinateDistance`). Add new props in `DioramaMapView.types.ts` + a `Prop` in `DioramaNativeModule.swift`; `FrameTicker` is reusable for T07.

### T05 Native city search (MKLocalSearchCompleter)
Status: done
Depends: T03, T04, T06
Files: modules/diorama-native/ios/DioramaSearch*.swift, modules/diorama-native/src/search.ts, src/features/cities/queries.ts, app/dev/search.tsx
Details: Async functions `autocomplete(query): Promise<Completion[]>` (restricted to cities and address results) and `resolve(completionId): Promise<City>` (name, country, lat, lon, suggested altitude from region span). TS hooks: `useCitySearch(query)` (debounced, `enabled: query.length > 1`, `placeholderData: keepPreviousData`) and `useCity(cityId)` (resolves curated or recent cities from the local cache without a network call). Add a dev-only route `app/dev/search.tsx` with a text field and a results list for checking.
Acceptance: A Simulator build compiles. In `diorama://dev/search`, typing "Par" returns Paris within about 300 ms. `useCity` works offline for curated cities (Jest test with the native module mocked).
Notes: "Par" → Paris 193–256 ms after last keystroke in the Simulator (debounce 100 ms). Resolved ids are `slug_lat_lon` (e.g. `paris_48.857_2.351`); suggested altitude ≈ place width / 12, clamped 800–3000 m. Filter = localities + neighborhoods + regions (Tokyo is filed as a prefecture); each query gets its own completer, newer cancels older. For T10: `useResolveCity().mutate(id)` → `addRecent` → navigate (the resolved city is already in the `useCity` cache); completions carry `titleHighlights`. NEEDS DEVICE CHECK — search latency on cellular; on iOS 16/17 street addresses also appear (no locality filter there).

### T06 Curated Flyover city list
Status: done
Depends: T01, T04
Files: src/features/cities/curated.ts, src/features/cities/__tests__/curated.test.ts
Details: About 20 cities known to have Apple 3D Flyover (New York, San Francisco, Chicago, London, Paris, Rome, Barcelona, Tokyo, Sydney, Las Vegas, Seattle, Boston, Berlin, Venice, Florence, Prague, Vancouver, Miami, Los Angeles, Dubai). Each entry has id, name, country, coordinates of a landmark-dense center, default altitude, pitch, heading, and an SF Symbol or accent for its row.
Acceptance: The list is typed. A Jest test checks that ids are unique and coordinates are in range. Spot-check 5 entries in 3D via `diorama://dev/map?lat=…&lon=…` (screenshots).
Notes: 20 cities; Amsterdam replaces Dubai (flat imagery only). Every center sits inside a T04-verified `FLYOVER_AREAS` entry (tested). `CuratedCity` = `RecentCity` + pitch, heading, symbol, tileColor; `getCuratedCity(id)`. Simulator spot-check of 5 camera framings still pending.

### T07 Head tracking (native)
Status: done
Depends: T04
Files: modules/diorama-native/ios/HeadTracker.swift, modules/diorama-native/ios/DioramaMapView.swift, modules/diorama-native/src/, app.json, app/dev/map.tsx
Details: `CMMotionManager.deviceMotion` at 60 Hz with `.xArbitraryCorrectedZVertical`. Convert the attitude to yaw/pitch/roll for a phone held in **landscape** in a head mount. Store a reference attitude when the viewer starts and on `recenter()`. Map yaw → camera heading offset. Map head pitch → camera pitch, clamped to the range MapKit allows, and looking down tilts you over the model. Map roll → rotate the container view with an overscanned frame. Smooth with a one-euro or low-pass filter. Drive everything from `CADisplayLink`, not the bridge. Add `debugLook` mode: a pan gesture feeds fake yaw/pitch for the Simulator (the dev map route turns it on). Add `NSMotionUsageDescription` to the app config.
Acceptance: On a device, turning your head turns the city with no visible jitter and no drift over 2 min. In the Simulator, dragging looks around.
Notes: NEEDS DEVICE CHECK — `npx expo run:ios --device`, open `diorama://dev/map?headTracking=1&landscape=1`: Xcode console should print `[HeadTracker] axis check OK`; in both landscape directions, head right turns right, look down tilts over the model, ear-to-shoulder stays level with no corners up to ~20°; no jitter; flat on a table 2 min, heading must not creep; Recenter; try `&sensitivity=0.5`/`2`. Pure pose math in `HeadPose.swift` (axis conventions at top), one-euro filter; yaw/pitch relative to the first-render/recenter pose, roll against gravity; props optional (default off/off/1). Overscan narrows the view (map is 1.68× screen height in landscape mono), so roll is capped at 20°. For T08: each eye needs its own rotating container, and apply both cameras only after both have loaded (terrain can shift the eyes vertically). Added the `@diorama/native` alias (tsconfig + Jest).

### T08 Stereo rendering (two eyes)
Status: done
Depends: T07
Files: modules/diorama-native/ios/DioramaMapView.swift, modules/diorama-native/ios/StereoRig.swift, modules/diorama-native/src/, app/dev/map.tsx
Details: With `mode: 'stereo'`, lay out two `MKMapView`s side by side, each at half width. For each eye, move the camera center perpendicular to the current heading by ±baseline/2, where baseline = altitude / 30 × eyeSeparation. Apply a small toe-in (convergence) so the model center has zero parallax. Set both cameras in the same display-link tick. Keep the viewer hidden behind a loading state until both eyes have fully rendered. `mode: 'mono'` goes back to a single view. Watch the thermal state: at `.serious`, drop to 30 fps; at `.critical`, switch to mono and emit `onDegraded`. The dev map route accepts `mode=stereo`.
Acceptance: A Simulator screenshot of `diorama://dev/map?mode=stereo` shows two eyes side by side with a small horizontal offset. Through a Cardboard viewer the city fuses into one 3D image and reads as a tabletop model. Changing eyeSeparation visibly changes the sense of scale.
Notes: NEEDS DEVICE CHECK — Cardboard: does `diorama://dev/map?mode=stereo&headTracking=1&eyeSeparation=0.5` (then 1, 2, 3) fuse and read as a tabletop model? Check fps and heat (each eye draws a 618-pt square plus a `CAReplicatorLayer` copy; T09 needs ≥50 fps), real head roll stays fused and level, thermal via Xcode Devices → Condition (serious → 30 fps, critical → mono + `onDegraded`), and that mono (preview, T07 tracking) did not regress. Design: both MapKit cameras look at the same center from eye positions (toe-in), then a per-eye homography warps back to an ideal parallel eye; this also cancels roll and zeroes parallax at the center. Needed because MapKit anchors each camera to the surface under its look-at point. Measured vertical disparity ≈0 pt in every case; horizontal disparity grows linearly with eyeSeparation. Layout margins are equalised (camera now centered on the full view, mono too); pitch cap is learned and shared by both eyes; switching to stereo builds fresh maps; `onReady` falls back after 10 s. T09 overlay slot: `EyeView.overlay` via `StereoRig.makeOverlay`.

### T09 Miniature (tilt-shift) look
Status: done
Depends: T08
Files: modules/diorama-native/ios/MiniatureOverlay.swift, modules/diorama-native/ios/DioramaMapView.swift, modules/diorama-native/src/
Details: Overlay each eye with a tilt-shift effect: stacked `UIVisualEffectView` blur bands at the top and bottom with gradient masks (`CAGradientLayer`), plus a sharp band in the middle. Add a slight saturation/contrast lift with a `CALayer` compositing filter or a tinted overlay. Private `CAFilter` APIs are forbidden (App Store). `miniatureIntensity` 0…1 scales the band size and blur. Keep the effect identical in both eyes.
Acceptance: At 0.6 intensity the view reads as a tilt-shift photo of a model (Simulator screenshot). No FPS drop below 50 on the target device with stereo on.
Notes: NEEDS DEVICE CHECK — fps with stereo at miniature 0.6 and 1 must stay ≥50; real head roll keeps the bands level; heat over a few minutes. Blur via the public `UIViewPropertyAnimator.fractionComplete` technique on `.systemUltraThinMaterialDark` (on iOS 26 it must be applied one runloop after the band is on screen; reapplied on foreground); below ~0.2 the bands fade in instead of blurring less. `compositingFilter` is unsupported on iOS, so the lift is a warm tint of up to 8%. Measured sharpness at 0.6: top 0.35×, middle 0.96×, bottom 0.16× of unblurred; the eyes match within 0.02. OPEN QUESTION for owner: the bottom band softens MapKit's Apple logo and Legal link, which Apple asks apps to keep visible.

## Phase 2 — Screens (React)

### T10 City picker screen
Status: done
Depends: T02, T03, T05, T06
Files: src/screens/CityPicker/**
Details: A native large title "Diorama" with the stack header's search bar (`headerSearchBarOptions`). When the search is empty, show a "Recent" section (if any) and a "Featured" inset-grouped list of curated cities. When searching, show results from `useCitySearch`. Tapping a search result resolves it, adds it to recents, then navigates, so `useCity` can find it offline. Row tap → haptic selection → `/city/[cityId]`. A settings gear sits top right in the header. Use skeleton rows while loading and a quiet empty state.
Acceptance: Search, tap a result, and you land on the preview. Recents update.
Notes: Search text lives in the URL (`?q=`), so `diorama://?q=par` opens with results; `useSeededSearchBarRef` fills the native field on first mount. Gear is a native `Stack.Toolbar` bar button (iOS 26 glass; Expo marks it experimental). Every opened city goes to Recent; a search result matching a featured city (same name, <50 km) opens the tuned featured entry. Leaving mid-resolve cancels the push. `ListRow` gained an optional `titleHighlights` prop. Simulator visual check pending (light/dark, `?q=par` bold matches, search field placement, VoiceOver "Settings").

### T11 City preview screen
Status: done
Depends: T04, T10
Files: src/screens/CityPreview/**
Details: A full-bleed mono `DioramaMapView` with `orbit` on, and the city name and country on a material card at the bottom. A capsule "Enter Diorama" button shows the SF Symbol `visionpro` (or `eyeglasses`) and a line of guidance: "Place your iPhone in your viewer." If the city doesn't have Flyover, show a subtle note: "3D buildings aren't available here. Terrain only."
Acceptance: Orbit is smooth, and the button navigates to `/view/[cityId]`.
Notes: Header is transparent; title = city name (so pushed screens read "‹ Paris") but not drawn over the map; blurred bar before iOS 26. Map stays opaque under a cover that fades out once rendered (MapKit may not draw an invisible map). Orbit pauses when the screen loses focus and under Reduce Motion (`useReduceMotion` follows the setting live; T14 could move it to `@/ui`). Terrain note only after `onReady` says no Flyover; unknown id shows an empty state. NEEDS DEVICE CHECK — orbit smoothness, card legibility over bright/dark imagery, 44-pt card radius vs the phone corners, haptic. Simulator visual check pending.

### T12 Viewer screen
Status: done
Depends: T08, T11
Files: src/screens/Viewer/**, src/features/viewer/**
Details: Landscape comes from the route's native stack `orientation` option (set in T03), backed by `expo-screen-orientation` if needed. Portrait comes back on exit. Keep the screen awake, hide the status bar and home indicator (`prefersHomeIndicatorAutoHidden` via the module if needed; keep any native change tiny and call it out). Show a 3-second countdown ("Put on your viewer"), then recenter. Double-tap anywhere recenters, with a haptic and a HUD. Long press (1 s) exits back to the preview. Pause rendering when the app goes to the background.
Acceptance: Full cycle on a device: enter, wear, look around, recenter, exit. No stuck orientation.
Notes: NEEDS DEVICE CHECK — full cycle: enter, wear, look around, double-tap recenter, hold 1 s to exit; the per-eye HUD copies fuse in the headset; an incoming call or the app switcher stops tracking and it recenters on return; VoiceOver "Exit"/activate actions. Tracking stays off during the 3-s countdown (which starts after `onReady`), then recenters and starts. The HUD is drawn once per eye in stereo. Exit uses `router.dismissTo` the preview (fixes the deep-link large-title collapse). Status bar hidden via expo-status-bar; `useKeepAwake`. No native pause: turning tracking off while inactive stops the per-frame work. Follow-up: `useReduceMotion` lives in CityPreview; move it to `@/ui` (T14).

### T13 Settings screen
Status: done
Depends: T02, T03, T04, T09
Files: src/screens/Settings/**
Details: An inset-grouped list: "Model size" slider (eyeSeparation 0.3–3), "Tracking sensitivity", "Miniature effect", a "Stereo" toggle, and "Look around by dragging" (debugLook, shown in dev builds only). Add a live mini preview at the top (mono DioramaMapView) that reflects the miniature setting. Add "Reset to defaults".
Acceptance: Changes persist across app reloads. The mini preview updates live (screenshots at miniature 0 and 1). The Viewer reads the same store, so changes apply there too (checked in T14).
Notes: Preview card with the Miniature slider directly under it (like Display & Brightness), then Stereo, Model size, Tracking sensitivity, Developer (dev only), and a red "Reset to defaults" (instant, light haptic). Every slider runs 0–1 through a scale: Model size is reversed (wider eyes = smaller-looking model) and spaced by ratio, and 1× sits mid-track for both. trackingSensitivity stays 0.5–2. Preview city = last opened featured city, else New York. `miniatureIntensity` is also wired into the Viewer. Follow-ups for T14: shared hooks (`useMapReveal`, `usePreviewOrbit`, `useReduceMotion`) live in CityPreview; rows import `@/ui/RowSeparator`/`rowStyles` directly; check the `camera.macro` slider icons. Simulator visual check pending.

## Phase 3 — Polish and ship

### T14 Apple polish pass
Status: done
Depends: T10, T11, T12, T13
Files: src/**, app/**
Details: Check Dynamic Type at XXL, VoiceOver labels and hints, Reduce Motion (turns off orbit and spring overshoot), dark mode, and haptics consistency. Also check copy and SF Symbol weights against the text weight. Confirm settings changes apply live in the Viewer. Confirm the `app/dev/*` routes redirect outside `__DEV__`.
Acceptance: A checklist in the task Notes with every item ticked.
Notes: Checklist: [x] VoiceOver (labels/hints/roles, rows as one element, Viewer Recenter/Exit actions + hint, failed-open announcement; tested) [x] Reduce Motion (one live `useReduceMotion` in `@/theme`; orbit off in Preview/Settings, verified by pixel diff; no spring bounce; HUD fade only) [x] Shared hooks in `src/features/map/`; `ControlRow`/`ToggleRow`/destructive `ListRow`/haptics in `@/ui` [x] Dynamic Type XXL + AX5 (rows wrap, value moves under title at AX sizes, buttons/HUD capped) [x] Dark mode [x] Haptics (selection rows, light Enter/Recenter/Reset, medium Exit) [x] Copy [x] SF Symbol weights [x] Settings apply live in Viewer [x] Dev routes redirect outside `__DEV__` [x] T02 separators match iOS 26 Settings exactly (72→369.7 pt, 1 pt) [x] T03 no hairline at rest [x] T06 framings (Sydney, Chicago retuned) [x] T10–T13 screens in light/dark. Fixed: HUD Liquid Glass not drawn under animated opacity, preview title drawn over map, terrain note clipped to 0 height, `?q=` links not syncing the search field. Open: T19, T20, T21. NEEDS DEVICE CHECK — spoken VoiceOver pass, haptic feel, live Reduce Motion toggle, HUD glass on a real iPhone.

### T15 Performance and thermals
Status: todo
Depends: T09, T12
Files: modules/diorama-native/**
Details: Profile with Instruments (Core Animation FPS, GPU, Energy) on a device. Tune the display-link rate, overscan size, and blur cost. Confirm the thermal fallback works.
Acceptance: 10 minutes in stereo without reaching `.critical` on an iPhone 15-class device. Record the numbers in Notes.

### T16 Tests
Status: done
Depends: T03, T05
Files: src/**/__tests__/, jest.setup.ts
Details: Jest is set up in T01. Add tests for the stores, query hooks (with the native module mocked), curated list integrity, and routes (each path renders its screen, using `renderRouter` from `expo-router/testing-library`). Add pure-TS tests for any math that lives in TS.
Acceptance: `npm test` is green.
Notes: Coverage of target code 93%→98% statements (features, providers, theme 100%; ui 100% lines). `jest.setup.ts` points expo-router testing-library's reanimated mock at the real Reanimated, so `useReducedMotion` works under `renderRouter`. Dev routes tested to redirect when `__DEV__` is false. Found a bug: malformed persisted recents crashed `getRecent()`, fixed as a T03 follow-up.

### T17 App icon, launch screen, TestFlight
Status: blocked
Depends: T14, T35
Files: app.json, assets/
Details: Icon (tiny isometric city block, Apple-style), a plain launch screen, Info.plist strings, and an EAS or Xcode archive configuration for TestFlight.
Acceptance: The build uploads to TestFlight.
Notes: Prep done and verified: isometric city-block icon (Icon Composer layers + flat fallback), light/dark splash marks, `appleTeamId` 3U62R986E5, buildNumber 1, iPhone only, `ITSAppUsesNonExemptEncryption` false. An unsigned Release archive builds (JS bundle embedded, icon compiled into Assets.car). BLOCKED ON OWNER — create the App Store Connect record for `canvas23studios.diorama`, then archive and upload in Xcode, or hand the orchestrator an App Store Connect API key (see `docs/testflight.md`). Bump `ios.buildNumber` for every upload.

### T20 Viewer loading cover lifts before both eyes are ready
Status: done
Depends: T08, T12
Files: modules/diorama-native/ios/DioramaMapView.swift, modules/diorama-native/ios/StereoRig.swift
Details: Found in the T14 visual pass: on a first, uncached visit (e.g. the retuned Sydney framing), the Viewer's black cover lifted and the city was visible for ~6 s with no countdown; `onReady` (and so the countdown) came later. The cover must stay up until the same moment `onReady` fires (both eyes fully rendered, or the 10-s fallback), so the cover, countdown and tracking start stay in sync. Reproduce cold with a cleared tile cache if possible.
Acceptance: On 5 cold first visits to uncached cities, the cover lifts at the same moment the countdown starts (Simulator screenshots/frame capture). Stereo alignment unchanged.
Notes: Stereo never lifted early (the cover and `onReady` already shared one code path); the gap was in mono and in a stereo view falling back to mono while loading, where the city showed 0.7–1.7 s before `onReady`. Now the cover depends only on readiness in every mode and lifts in the same update that sends `onReady` (measured 0 ms; JS countdown 2–8 ms later). Stereo fades over 0.3 s; mono lifts instantly, so the preview/Settings reveals are unchanged. The 1.5× zoom fix still holds. NEEDS DEVICE CHECK — with Stereo on and off, entering an unopened city stays black until the city and "3" appear together.

### T21 Keep MapKit attribution legible under the miniature blur
Status: blocked
Depends: T09
Files: modules/diorama-native/ios/MiniatureOverlay.swift
Details: The bottom tilt-shift band blurs MapKit's Apple Maps logo and "Legal" link (Viewer, Settings preview, dev map). Apple requires map attribution to stay visible, so this is an App Review risk. Options: keep a small sharp strip at the bottom of each eye where the attribution sits, or move the attribution above the band. Owner decides.
Acceptance: The logo and "Legal" are legible in every eye at miniature 1 (screenshots), and the tilt-shift still reads as a model.
Notes: Waiting on owner decision.

### T22 Cardboard-style eye layout (lens-centered viewports)
Status: done
Depends: T20
Files: modules/diorama-native/ios/StereoRig.swift, modules/diorama-native/ios/EyeView.swift, modules/diorama-native/ios/DioramaMapView.swift, modules/diorama-native/ios/ViewerProfile.swift, modules/diorama-native/ios/DioramaNativeModule.swift, modules/diorama-native/src/, src/screens/Viewer/PerEye.tsx
Details: The owner tried stereo on an iPhone 14 Pro in a headset and could not fuse the two images. Today each eye fills half the landscape screen edge to edge, so the eye centers sit about 70 mm apart (wider than the ~64 mm between viewer lenses) and the picture runs into the lens edges. Match a Cardboard-style viewer instead (reference: `docs/reference/cardboard-viewer.png`; ignore its green night-vision color): each eye is a smaller viewport centered on its lens, with black around it. Horizontal centers at screen center ± lensSpacing/2 (default 64 mm, Cardboard v2), vertically centered; convert mm to points with a small per-device points-per-mm table (there is no public PPI API; use the `utsname` model id, with a sensible fallback). Viewport size roughly the reference's proportions (each eye ~54% of a half-screen wide, ~48% of the screen tall), clamped so the two never overlap. Expose `lensSpacing` (mm) as an optional prop with that default. Keep T08's per-eye homography/roll pipeline inside each viewport and recompute overscan for the smaller size. Tune the default `eyeSeparation` so both eyes show nearly identical content like the reference (small disparity); measure and report the max edge disparity in points. Emit the two viewport rects to JS (e.g. an `onEyeLayout` event) so `PerEye` centers the countdown/"Recentered" HUD in each viewport instead of each half-screen. Mono stays full screen. No barrel distortion here (that is T18).
Acceptance: A Simulator screenshot of the stereo Viewer shows two black-surrounded viewports whose centers are lensSpacing apart (measured in points, matching the device table), with the HUD centered in each; stereo vertical alignment still ≈0 pt. NEEDS DEVICE CHECK — the owner fuses the two images in their headset.
Notes: NEEDS DEVICE CHECK — in the headset, do the two windows fuse, and is depth comfortable at Model size 1×? Head roll stays level inside the windows; heat and fps with two maps. iPhone 14 Pro expected: lens centers at x = 232.83 / 619.17 pt, y = 196.5 pt (386.33 pt = 64.0 mm apart); windows 229 × 187 pt at x = 118.33 / 504.67, y = 103. Each eye is a 38 × 31 mm window with black around it; points per mm from a per-model ppi table (`ViewerProfile.swift`). Baseline lowered from altitude/30 to /50: max edge disparity at 1× went 14.3 → 4.0 pt. MapKit caps pitch at 35° on maps the window's size, so each eye still draws a 661-pt map scaled ~0.465 into its window (pitch 60° kept, no GPU saving, Apple logo/Legal shrink to ~47%: relevant to T21). New `lensSpacing` prop (mm, default 64) and `onEyeLayout` event; `PerEye` centers the HUD from `useStereoEyes()`. Tip: Metro started with `CI=1` does not watch files; start it without `CI` for Simulator checks.

### T18 Barrel (lens) distortion pre-correction per eye
Status: blocked
Depends: T22, T24, T26
Files: modules/diorama-native/ios/
Details: The reference (`docs/reference/cardboard-viewer.png`) pre-distorts each eye with a barrel warp (bulged edges) so straight lines look straight through the viewer's lenses. Start from Cardboard v2 coefficients (k1 = 0.34, k2 = 0.55). The current per-eye picture is a `CAReplicatorLayer` copy with a projective `CATransform3D`, which can't bend lines, and `CAMeshTransform` is private (forbidden). Research first and pick the cheapest public path, e.g. a grid of clipped replicator tiles each with its own local projective transform (piecewise-projective approximation of the warp) vs capturing the map into a Metal texture and warping it with a mesh. Report the cost before building; drop it if fps falls below 50 with stereo and miniature on.
Acceptance: Simulator screenshots show the reference's bulged-edge eye shape, and the fps budget holds on device. NEEDS DEVICE CHECK — straight lines look straight through a Cardboard lens.
Notes: Dropped after research: no public API renders MapKit into something we can warp. A prototype with an 8×8 mesh of replicator copies (0.26 pt from the exact Cardboard warp, k1 0.34 / k2 0.55) ran at ~18 fps vs 57 without it in the Simulator (each copy of the MapKit layer costs ~0.25–0.3 ms of render-server time); 2×2/4×4 meshes stay near budget but are too coarse (≥1 pt error). SwiftUI `Map` rejects shader effects (`.drawingGroup()` shows the "not supported" placeholder); `drawHierarchy` takes 16–27 ms per eye per frame. Uncorrected pincushion at 35 mm / 40 mm lens is mild (edge ~8.5% farther out, a street 80 pt off-center bows ~0.4°). Revisit if Apple adds shader effects for `Map` or render-to-texture. If revived: lens gain unchanged, eye map ~8.5% larger.

### T23 First-person head tracking (look around from a fixed vantage point)
Status: done
Depends: T20
Files: modules/diorama-native/ios/FirstPersonCamera.swift, modules/diorama-native/ios/DioramaMapView.swift, modules/diorama-native/ios/StereoGeometry.swift, modules/diorama-native/ios/StereoRig.swift, OVERVIEW.md
Details: The owner says head movement feels like a third-person move. Today head yaw adds to the camera `heading` around the fixed look-at center (`DioramaMapView.swift`, "yaw turns the heading"), so the city spins like a turntable, and head pitch swings the camera over it. Make it first person: the head stays at a fixed vantage point (the eye position of the starting camera, from its center, altitude as camera-to-center distance, pitch and heading), and turning the head rotates the gaze from there, so the city stays world-fixed and the view pans across it. Per frame: gaze heading = base heading + yaw × sensitivity, gaze pitch = base pitch + head pitch; aim point = where the gaze ray meets the ground; place the camera with `MKMapCamera(lookingAtCenter:fromEyeCoordinate:eyeAltitude:)`. Near the horizon the aim point runs away, so clamp to MapKit's pitch cap (T08 learns it) and keep it smooth. Stereo: the eyes sit ±baseline/2 either side of the fixed head position, perpendicular to the current gaze heading, both converging on the aim point; keep T08's homography/roll pipeline and its surface-anchoring notes, with zero vertical disparity. Recenter re-zeroes the head reference; the vantage point does not move. The preview orbit (no tracking) stays as is. Put the math in a pure, commented type and prove it with a scratch `swiftc` harness. Update OVERVIEW.md's motion description. Owner feedback (second report): "the movement should feel like you're moving your head; it feels like I'm moving in a circle, like on a drone". So also: (1) calibrate the gain so a head turn of N° moves the scene N° as seen through the lens. MapKit renders 30° vertical, but the eye window subtends a different angle through the lens (derive it from the window height in mm and a lens-to-screen distance, default ~40 mm), so the camera rotation must be scaled by rendered-FOV ÷ perceived-FOV at sensitivity 1×; (2) no perceptible lag: tune the one-euro filter for low latency on rotation (smoothing only for jitter at rest) and apply the pose in the same display-link tick it is read.
Acceptance: In the Simulator (`debugLook`), dragging right pans the view right while the eye coordinate stays constant (temporary log or harness), and landmarks translate across the screen instead of rotating about the center (before/after screenshots); stereo vertical disparity ≈0 pt. NEEDS DEVICE CHECK — turning the head feels like looking around from a fixed spot above the model.
Notes: NEEDS DEVICE CHECK — in the headset, turning your head should feel like standing still above a table model and looking around (no circling or drone feel); a ~20° turn moves a building about as far as your head turned, with no lag; looking up stops gently (the start view is already at MapKit's steepest pitch); recenter swings back without moving you; over dense Midtown watch for a slight up-and-down bob. Design: a fixed vantage point from the starting camera, with gaze = base + head yaw/pitch, and the aim point where the gaze meets the ground. Stereo eyes at ±baseline/2 around the vantage point, converging on the aim point; vertical disparity ≈0.06 pt max. Lens gain = rendered FOV ÷ perceived FOV (31-mm window: 18.5° vs 42.3° → 0.437; mono ≈0.23; lens distance 40 mm in `ViewerProfile`). Filter tuned: lag 26→9 ms at 20°/s, worst start-of-turn 43→10 ms, 100 Hz sampling. Limit: MapKit stands the camera on the terrain height under the aim point (no public API to read it), so over tall downtowns the vantage bobs ~±10 m on 1 km. T24 must re-check the gain for taller windows.

### T24 Fit the eye windows to the headset's lens holes (taller, narrower, adjustable)
Status: done
Depends: T22, T23
Files: modules/diorama-native/ios/ViewerProfile.swift, modules/diorama-native/ios/StereoRig.swift, modules/diorama-native/ios/EyeView.swift, modules/diorama-native/ios/DioramaNativeModule.swift, modules/diorama-native/src/, src/features/settings/store.ts, src/screens/Settings/**, src/screens/Viewer/ViewerMap.tsx
Details: On the owner's headset each lens hole is about 34 mm wide, and T22's 38 × 31 mm eye windows get clipped at the sides. Make the windows taller and no wider than the hole: default width 33 mm (just inside 34) and a taller height (default ~42 mm, clamped to the screen's short side minus a margin), still centered on each lens and never overlapping. Expose window width and height (mm) as optional native props next to `lensSpacing`, and add a "Viewer fit" section in Settings with sliders for Lens spacing (55–72 mm), Window width (25–40 mm) and Window height (25–60 mm), persisted in the settings store and applied live in the Viewer, so the owner can tune the fit on the phone without a rebuild. Keep T22's pitch-cap workaround (each eye draws a full-size map scaled into its window), recompute the scale and overscan for the new aspect, and keep vertical disparity ≈0 pt and the HUD centered in each window (`onEyeLayout`).
Acceptance: With defaults, a Simulator screenshot shows windows 33 mm wide and taller than wide (measured in points against the device table); changing the Settings sliders resizes the windows live in the Viewer; stereo vertical alignment ≈0 pt; typecheck, lint and tests pass. NEEDS DEVICE CHECK — in the owner's headset nothing is clipped by the 34 mm holes.
Notes: NEEDS DEVICE CHECK — in the headset with defaults, nothing is clipped by the 34-mm holes and the two images fuse; a ~20° head turn still moves the city ~20° (gain now 0.41). Default windows are 33 × 42 mm (iPhone 14 Pro: 199 × 253 pt at x = 133.33 / 519.67, y = 70). Width is capped at the lens spacing (no overlap), height at the screen minus 2 mm. New store settings `lensSpacing` 55–72, `windowWidth` 25–40, `windowHeight` 25–60 (defaults 64/33/42); old saves get the defaults through the merge (version kept at 1 on purpose). A "Viewer fit" section in Settings has whole-mm sliders; it dims in mono and applies live in the Viewer. Each eye now draws a 316 × 402-pt map scaled 0.63; pitch stays 60°; vertical disparity ≈0.06 pt; the gain follows the live window height. Owner tuning: set Lens spacing to the measured lens-center distance, lower Window width 1 mm at a time until both side edges show, raise Window height until the top and bottom start to clip, then back off 1 mm.

### T26 Circular eye windows, 35 mm across
Status: done
Depends: T24
Files: modules/diorama-native/ios/ViewerProfile.swift, modules/diorama-native/ios/EyeView.swift, modules/diorama-native/ios/StereoRig.swift, modules/diorama-native/ios/DioramaMapView.swift, modules/diorama-native/ios/DioramaNativeModule.swift, modules/diorama-native/ios/MiniatureOverlay.swift, modules/diorama-native/src/, src/features/settings/store.ts, src/screens/Settings/**, src/screens/Viewer/ViewerMap.tsx, src/screens/Viewer/PerEye.tsx
Details: The owner wants each eye's viewing area to be a circle 35 mm in diameter (matching the round lens holes), centered on its lens, black outside. Mask each eye window to a circle (a `CAShapeLayer` mask or `cornerRadius` on a square container; public API only), sized from a `windowDiameter` in mm (default 35) that replaces T24's separate width/height: native prop `windowDiameter` (clamped, capped so the two circles never overlap and fit the screen height), store setting `windowDiameter` (25–45 mm, default 35) replacing `windowWidth`/`windowHeight` (migrate old saves: drop the old keys, default the diameter), and the "Viewer fit" section shows Lens spacing and Diameter sliders. Keep T22's pitch-cap workaround (a full-size map scaled into the window) with the square window = diameter, and T23's lens gain following the window height (= diameter). The tilt-shift bands (`MiniatureOverlay`) stay inside the circle. MapKit draws its Apple Maps logo and "Legal" link in the map's bottom corners, which a circle would cut off: move them inside the circle via each eye's attribution/layout insets so they stay visible (App Review; see T21). The HUD stays centered in each circle (`onEyeLayout` reports the square bounding each circle).
Acceptance: A Simulator screenshot of the stereo Viewer shows two black-surrounded circles, 35 mm across (measured in points against the device table) and 64 mm apart center to center, with the Apple logo and "Legal" visible inside each circle and the HUD centered; the Diameter slider resizes the circles live; vertical disparity ≈0 pt; typecheck, lint and tests pass. NEEDS DEVICE CHECK — in the headset the circles fill the round lens holes without clipping.
Notes: NEEDS DEVICE CHECK — in the headset each circle fills its round lens hole with no clipping, the two images fuse, and a ~20° head turn still moves the city ~20° (lens gain now 0.61). iPhone 14 Pro: circle centers at x = 232.83 / 619.17 pt, y = 196.5 pt, diameter 211 pt (34.95 mm). Circles are clipped with a public mask; `windowDiameter` (25–45, default 35) replaces width/height, and old T24 saves drop those keys. Viewer fit now has Lens spacing + Diameter. A circle is roll-invariant, so each eye's map only covers the diameter (419 pt at scale 0.525, ~38% fewer pixels than T24); pitch stays 60°; vertical disparity ≤0.05 pt. The Apple logo and Legal sit at the circle's 45° bottom-left via layout margins and stay inside under roll. Owner tuning: set Lens spacing to the measured lens-center distance, lower Diameter until the whole edge shows, raise it until the edge just touches the lens rim, then back off 1 mm.

### T27 A visible, reliable way to exit the Viewer
Status: done
Depends: T12
Files: src/screens/Viewer/ViewerScreen.tsx, src/screens/Viewer/ExitButton.tsx, src/screens/Viewer/CountdownHud.tsx, src/screens/Viewer/ViewerGestures.tsx, src/screens/Viewer/__tests__/, src/features/viewer/useExitViewer.ts, src/features/viewer/useViewerHud.ts
Details: The owner, using the Release build on their iPhone, found no way to leave the Viewer ("there's no way to go back"). T12's only exits are an undiscoverable 1-second hold anywhere and VoiceOver actions, and the hold may not be firing on device. Add a visible exit: a small glass close button (SF Symbol `xmark`, `GlassButton`, 44-pt hit target, VoiceOver label "Exit") in the top-left corner, inside the safe area. In stereo it sits in the black margin outside the eye windows/circles (use the `useStereoEyes()` rects so it never overlaps them), so it stays visible when the phone is out of the headset but can't be seen through the lenses; it is always shown. In mono it fades in when the screen is tapped once and fades out after 3 s (a single tap must not delay or break the double-tap recenter; use `Gesture.Exclusive` ordering or `requireExternalGestureToFail`). Tapping it exits exactly like the hold (`useExitViewer`: back to the preview, portrait restored, medium haptic). Keep the 1-second hold, and make sure it can fire: allow some finger drift (`maxDist` ~30 pt) and check nothing in the native view swallows the touch. Add a one-line hint under the countdown ("Hold anywhere to exit"). Respect Reduce Motion for the fades.
Acceptance: Jest: the exit button renders in stereo outside both eye rects, exits to the preview when pressed, and in mono appears on a single tap and hides after 3 s without breaking double-tap recenter; the hold still exits. A Simulator screenshot of the stereo Viewer shows the button in the black margin, not over an eye. NEEDS DEVICE CHECK — on the iPhone the X exits the Viewer, and holding a finger down for a second also exits.
Notes: NEEDS DEVICE CHECK — on the iPhone the ✕ exits, and a 1-s hold exits even with a little finger drift; in mono one tap shows the ✕ (after the double-tap wait) and it hides after 3 s, while double-tap still recenters at once; the ✕ cannot be seen through the lenses; it reads well in light and dark. The ✕ is a dark `GlassSurface` (Liquid Glass does not draw under a fading parent) placed 16 pt in from the safe-area top-left, sliding left of or above the left eye if it would crowd it. The hold most likely failed because RNGH long press cancels after 10 pt of drift; `maxDist` is now 30 pt. Nothing native swallows touches. The countdown shows "Hold anywhere to exit". Simulator screenshot pending (after T26).

### T25 Custom locations: search any address or landmark via Apple Maps
Status: done
Depends: T05, T10, T11
Files: modules/diorama-native/ios/DioramaSearch.swift, modules/diorama-native/ios/DioramaSearchRecords.swift, modules/diorama-native/src/search.ts, src/features/cities/queries.ts, src/screens/CityPicker/**, app/dev/search.tsx
Details: The owner wants to open any place, not just cities: type a street address ("1 Infinite Loop, Cupertino"), a landmark or a business, look it up through Apple Maps, and view it as a diorama. Today T05's completer is limited to cities (`resultTypes = .address` with an iOS 18 `addressFilter` for localities, neighborhoods and regions), so street addresses and points of interest never show. Widen the search: include full addresses and points of interest (`.pointOfInterest`), keeping cities. Each completion reports its kind (city / address / place) so the picker can show them in separate inset-grouped sections ("Cities", "Places") with fitting SF Symbols. `resolve` returns the same `City` shape for every kind: name = the place's title, country/subtitle from the placemark, coordinates from the map item, and a suggested altitude that suits the kind (an address or landmark frames a block or two: ~600–900 m camera distance; cities keep T05's span-based value). Ids stay URL-safe (`slug_lat_lon`) so resolved places go into Recent and reopen offline through `useCity`. Places outside Flyover coverage open normally and show T11's terrain note. Update `app/dev/search.tsx` to show the kind. Keep the debounce and stale-result handling from T05.
Acceptance: In the Simulator, `diorama://?q=1%20infinite%20loop` shows the address under "Places", tapping it opens the preview framed on that block, and it appears in Recent; a landmark query (e.g. "Golden Gate Bridge") works the same way; city searches still work ("Par" → Paris under "Cities"); Jest covers the kind mapping and the altitude rules with the native module mocked; typecheck, lint and tests pass.
Notes: NEEDS DEVICE CHECK — type through two or three searches at normal speed and confirm "Can't search right now" never appears (MapKit allows ~100 completer requests/min, and it takes ~60 s to recover); check the 700 m / 900 m framing in the stereo Viewer. Two searches per keystroke: cities (T05) + everything else (addresses, POIs, natural features; skipping countries, postcodes, parking/EV/ATM/restrooms). Kind: in the cities list → city; else a subtitle with a digit → place, otherwise address; refined on resolve (a POI category → place). Sections "Cities"/"Places", ordered by best title match. Addresses open at 700 m, places at 900 m; subtitle "Town, Country"; the search placeholder is "Search for a city or place". Dev route gained `open=1`. Added San Jose to `flyoverCoverage.ts`. Follow-ups: T28 (false terrain note outside the listed metros), T29 (recents drop entries with an empty subtitle). Golden Gate Bridge is filed as a street, so it opens at 700 m on a stretch of deck.

### T28 Don't claim "no 3D buildings" for places outside the curated coverage list
Status: done
Depends: T25
Files: modules/diorama-native/src/flyoverCoverage.ts, modules/diorama-native/src/DioramaMapView.tsx, modules/diorama-native/src/DioramaMapView.types.ts, modules/diorama-native/src/__tests__/, src/screens/CityPreview/TerrainNote.tsx, src/screens/CityPreview/usePreviewMapStatus.ts, src/screens/CityPreview/__tests__/
Details: `flyoverAvailable` comes from a hand-kept list of ~60 metros (T04; MapKit has no API for it). Now that any address can be opened (T25), a place outside the list shows "3D buildings aren't available here. Terrain only." even where Apple has 3D (T25 had to add San Jose for 1 Infinite Loop). Make coverage three-state: `yes` (inside a listed area), `no` (inside a known-flat area: Dubai and Mexico City were checked flat in T04; keep a small list), `unknown` (everything else). Show the terrain note only for `no`; show nothing for `unknown`. Update the `onReady` event type and its docs, the preview's note logic and the tests.
Acceptance: Jest: a curated city → `yes`, no note; Dubai → `no`, note shown; a point far from every listed area → `unknown`, no note. typecheck, lint and tests pass.
Notes: The `onReady` event now carries `coverage: 'yes' | 'no' | 'unknown'` (replacing the `flyoverAvailable` boolean): `yes` inside `FLYOVER_AREAS`, `no` inside the new `FLAT_AREAS` (Dubai and Mexico City, 30-km radii as an estimate), `unknown` elsewhere. The terrain note shows only for `no`. The dev map title shows the three states. Follow-up: the comment at `DioramaMapView.swift` lines 17–18 still says `flyoverAvailable`. NEEDS DEVICE CHECK — Dubai shows the terrain note, Reykjavík (unknown) shows none.

### T29 Keep places with no town or country in Recent across relaunches
Status: done
Depends: T25
Files: src/features/cities/recentsStore.ts, src/features/cities/__tests__/recentsStore.test.ts
Details: The recents store's persisted-data sanitiser (T03 follow-up) requires a non-empty `country`, so a resolved place whose subtitle is empty (rare, but possible for T25's addresses and POIs) silently drops out of Recent after a relaunch. Allow an empty (or whitespace) `country` and store it as an empty string; still require a non-empty `id` and `name` and valid coordinates and altitude.
Acceptance: Jest: an entry with an empty country survives a persist → reload round-trip; malformed entries are still dropped. typecheck, lint and tests pass.
Notes: A blank or whitespace `country` is kept and stored as `''`; `id`/`name` must still be non-blank with valid coordinates and altitude. `ListRow` and `PreviewCard` already skip an empty subtitle. NEEDS DEVICE CHECK — a Recent row with an empty subtitle shows only its name and survives a relaunch.

### T30 Camera height slider (closer or higher up)
Status: done
Depends: T23, T26
Files: src/features/settings/store.ts, src/features/settings/__tests__/store.test.ts, src/screens/Settings/**, src/screens/Viewer/ViewerMap.tsx, src/screens/CityPreview/PreviewMap.tsx, modules/diorama-native/ios/FirstPersonCamera.swift
Details: The owner wants to choose how high the camera is, to get closer to the city or higher above it. Add a "Camera height" setting: a multiplier on each place's own `altitude` (camera-to-center distance), range about 0.4×–3×, default 1×, ratio-spaced so 1× sits mid-track like Model size, with small/large end symbols (e.g. `arrow.down.to.line` / `arrow.up.to.line` or `airplane`), clamping the resulting distance to roughly 300–5000 m. Put it in Settings (its own row near Model size, not in "Viewer fit"), persist it, reset it with "Reset to defaults", and apply it live in the Viewer (stereo and mono), the preview's orbit map, and the Settings mini preview. The stereo baseline already scales with altitude (altitude/50 × eyeSeparation), so depth stays proportional; T23's first-person vantage point must be recomputed when the height changes (new vantage), and recenter must keep working. MapKit caps pitch by altitude (T23 measured 70° at ~1 km, 60° at 1.5–2 km, ~35° from ~2.5 km on the eye map size), so a higher camera looks more top-down: note it in the slider's footer ("Higher views look more straight down."). Run after T18 (shared settings/Viewer files).
Acceptance: Changing Camera height moves the camera closer/farther live in the Viewer and preview (Simulator screenshots at 0.5×, 1× and 2×, same city); the setting persists and resets; stereo vertical disparity stays ≈0 pt; typecheck, lint and tests pass. NEEDS DEVICE CHECK — in the headset, lower feels closer to the model and higher feels farther above it, with comfortable depth at both ends.
Notes: NEEDS DEVICE CHECK — in the headset 0.5× feels closer to the model and 2–3× farther above it, with comfortable depth at both ends; at 2×+ the first head movement after the countdown does not make the city jump or slide away. `cameraHeight` 0.4–3 (default 1, ratio-spaced; 1× sits at 45% of the track); effective altitude = clamp(place × h, 300, 5000) m via `src/features/map/cameraHeight.ts`; applied live in the Viewer, preview and Settings mini preview. Native fix (outside Files): at 2× MapKit only draws 35° but the vantage was built from 60°, aiming ~1.1 km short, so while tracking the start camera now uses the steepest pitch MapKit draws at that distance (cached `steepestPitch(lookingAt:)` in StereoRig). Drawn stereo pitch for Paris: 0.5× 60°, 1× 60°, 2× 35°. Vertical disparity ≤0.001 pt at every height.

### T31 Fully free look-around: up past the horizon to straight up, any roll
Status: done
Depends: T23, T26, T30
Files: modules/diorama-native/ios/FirstPersonCamera.swift, modules/diorama-native/ios/StereoGeometry.swift, modules/diorama-native/ios/StereoRig.swift, modules/diorama-native/ios/EyeView.swift, modules/diorama-native/ios/HeadPose.swift, modules/diorama-native/ios/DioramaMapView.swift, modules/diorama-native/ios/SkyBackdrop.swift, OVERVIEW.md
Details: The owner wants the camera fully movable: "Do not lock the horizon at the angle that it appears to be locked to. I want to be able to fully look around, straight up if I want." Today T23 eases the gaze to a stop at MapKit's pitch cap (~60–70° from straight down on the eye map size), and MapKit can never look above the horizon. Because first-person look is a pure rotation about a fixed eye point, any extra rotation is exactly a homography of the rendered picture, whatever the depth. So: drive MapKit's camera up to its cap (learned per vantage, as now), and apply the remaining gaze pitch as an extra rotation in each eye's existing homography (T08's per-eye warp), with no gentle stop. MapKit only renders a 30°-tall slice, so above the top of the rendered picture there is no map content: fill it with a sky backdrop (`SkyBackdrop.swift`: a gradient matched to MapKit's own sky/haze color at the horizon, brightening toward the zenith, identical in both eyes since it sits at infinity) behind the warped picture, blended at the seam so no hard edge shows. Looking straight up shows only sky; looking back down returns smoothly to the real city. Yaw stays unlimited (full 360°). T07's 20° roll limit is still in code (`maxRollDegrees = 20.0` in `DioramaMapView.swift`); check whether it is still needed with T26's circular windows (a circle is roll-invariant and each eye's map covers the diameter); if so, remove the limit so roll is free too. Keep vertical disparity ≈0 pt, the lens gain, the tilt-shift overlay (it should fade out over pure sky), attribution legibility, and the HUD. Research first: measure MapKit's actual pitch cap and whether it draws its own sky near the cap at the current map size, and report. Document the MapKit limit honestly in OVERVIEW.md: the band between the rendered slice and the true horizon is synthesized haze, not real distant city.
Acceptance: In the Simulator (debugLook), dragging up keeps tilting smoothly past the old stop, through the horizon, to straight up (all sky), and back down to the city with no jump or seam (screenshots at gaze 45°, cap, horizon, 45° up and 90° up); stereo vertical disparity ≈0 pt at each; roll is not clamped (or the reason it must stay clamped is reported); typecheck, lint and tests pass. NEEDS DEVICE CHECK — in the headset you can look all the way up and around without hitting a stop, and the sky join feels natural.
Notes: NEEDS DEVICE CHECK — in the headset, look all the way up and around with no stop; the city → haze → sky join feels natural; the faster turning past ~30° of nod feels OK; sideways head tilt (even onto a shoulder, and while looking up) keeps the horizon level. MapKit follows the gaze up to its cap and each eye's homography turns the picture the rest of the way (exact at every depth); pitch runs 0–180°, yaw unlimited. MapKit never draws above ~11–40° below the horizon (cap 70° to ~700 m, 60° at 0.9–1.25 km, 35° from 1.5 km) and draws no sky/haze of its own, so `SkyBackdrop.swift` paints a daylight gradient by true elevation and the map's top edge fades into it; that band is synthesized haze, not distant city (documented in OVERVIEW). Gain is exact for ±30° of nod, then bends so a 90° nod reaches straight up. The map hides before any corner could swing behind the eye (no ghost city). Stereo roll is unlimited; mono keeps 20° (a free roll would need a 42% bigger map). Vertical disparity ≤0.14 pt across the sweep. The Apple logo stays until ~12° past the cap (MapKit hides it in small margin boxes).

### T32 Orientation picks the mode: portrait = full screen, landscape = two-eye VR
Status: done
Depends: T12, T26, T27, T30
Files: app/_layout.tsx, src/screens/Viewer/**, src/features/viewer/**, src/features/settings/store.ts, src/features/settings/__tests__/store.test.ts, src/screens/Settings/**, src/screens/CityPreview/PreviewCard.tsx, src/screens/CityPreview/__tests__/
Details: The owner wants the Viewer's layout to follow how the phone is held: portrait is a plain full-screen experience, landscape is the two-eye headset (VR) view. Today the Viewer route is locked to landscape (T03 native-stack `orientation`) and stereo vs mono is a Settings toggle. Change it: the Viewer route allows portrait and both landscapes (not upside down); portrait shows one full-screen mono map, held like a window into the city (head tracking still turns the view as you move the phone, plus a one-finger drag to look around, and no countdown: it starts at once, with the ✕ exit and double-tap recenter); landscape shows the stereo circles with T12's countdown ("Put on your viewer"), the ✕ in the margin and hold-to-exit. Rotating while in the Viewer switches live: keep the same vantage point and head reference, keep the loading cover up while the new eyes render (T20), recenter after the switch, and don't regress the rotation zoom fix (T08 follow-up). Replace the Settings "Stereo" toggle with "Two-eye view in landscape" (default on; off means landscape is mono full screen too) and migrate the old `mode` setting. Update the preview's guidance line to say the headset view is sideways (e.g. "Turn your iPhone sideways and place it in your viewer."). Keep all mono/stereo props going through the existing native `mode` prop; native changes only if rotation handling needs them (keep them tiny and call them out).
Acceptance: In the Simulator, Enter Diorama in portrait opens a full-screen mono view with no countdown; rotating to landscape switches to the two circles with the countdown, and back to portrait returns to full screen, with no zoom jump, a correct framing and working recenter both ways (screenshots of each); with "Two-eye view in landscape" off, landscape is full-screen mono; the setting migrates from the old `mode`; typecheck, lint and tests pass. NEEDS DEVICE CHECK — hold the phone upright for a normal full-screen look, turn it sideways into the headset for the two-eye view.
Notes: NEEDS DEVICE CHECK — upright: Enter Diorama opens full-screen mono with no countdown, moving the phone turns the view, a one-finger drag looks around, a tap shows the ✕; sideways: black cover → circles + "Put on your viewer" countdown → recenter; back upright: full screen, recentered, no zoom jump; with "Two-eye view in landscape" off, sideways stays mono. The Viewer route turns every way but upside down (`orientation: 'default'`). `mode` migrates to `twoEyeLandscape`. Native follow-ups landed with T31: the drag adds to the motion look (`HeadTracker`), native `onReady` carries `{ mode }` so JS ignores a stale mono report, and the thermal fallback sends `onDegraded` before `onReady`. Simulator rotation round-trip checked: pixel-identical, 0.00 px shift.

### T33 Swipe to delete suggested and custom locations in the picker
Status: done
Depends: T10, T25
Files: src/ui/SwipeToDeleteRow.tsx, src/ui/index.ts, src/ui/__tests__/, src/features/cities/hiddenFeaturedStore.ts, src/features/cities/__tests__/, src/screens/CityPicker/FeaturedSection.tsx, src/screens/CityPicker/RecentSection.tsx, src/screens/CityPicker/__tests__/, src/screens/Settings/**
Details: The owner wants to remove places from the picker with the standard iOS row swipe: both the suggested ("Featured" curated cities) and their own custom locations (searched cities, addresses and places in "Recent"). Add a reusable swipe-to-delete row in `@/ui` (react-native-gesture-handler's `ReanimatedSwipeable`, already installed): swiping left reveals a red "Delete" action (SF Symbol `trash`), a full swipe deletes, with a light haptic, a spring collapse of the row (respect Reduce Motion), and a VoiceOver custom action "Delete". Recent: delete calls `removeRecent(id)`. Featured: curated cities are built in, so "delete" hides them via a new persisted store of hidden featured ids (zustand + MMKV, sanitised like the other stores), and the Featured section filters them out (hide the section header when all are hidden). Add a "Restore suggested places" row in Settings (shown only when something is hidden) that clears the hidden list. Only one row may be open at a time; tapping elsewhere or scrolling closes it; a tap on a closed row still opens the city. Keep row heights, separators and inset-grouped corners intact during and after the swipe.
Acceptance: Jest: swiping/deleting a Recent row removes it from the store and the list; deleting a Featured row hides it, persists across a reload, and "Restore suggested places" brings it back; the VoiceOver "Delete" action works; a tap still opens the city. Simulator screenshots of a row mid-swipe (red Delete revealed) in light and dark, and the list after a delete. typecheck, lint and tests pass.
Notes: NEEDS DEVICE CHECK — a real finger swipe: the reveal follows the finger, a release past half the action width stays open, a full swipe ticks then deletes on release; tapping another row, the background, or scrolling closes an open row; a mostly-vertical scroll starting on a row still scrolls; the VoiceOver "Delete" action; under Reduce Motion a deleted row fades. `SwipeToDeleteRow` in `@/ui` (`ReanimatedSwipeable`), one open row at a time via `openSwipeRow`. Recent → `removeRecent`; Featured → hidden via `hiddenFeaturedStore` (persisted, sanitised). The Settings "Restore suggested places" row shows only when something is hidden; Reset to defaults leaves hidden cities alone. Hidden cities still open from search and links. If everything is hidden and Recent is empty, the picker shows "No places" pointing to Settings. `jest.setup.ts` maps gesture-handler to its TS source (removes ~290 worklet warnings). Fixed a UI-thread `springConfig` crash seen only in the Simulator.

### T34 Use iOS 27 MapKit for the highest-quality maps (incl. Apple's new Gaussian-splat 3D)
Status: todo
Depends: T08, T28, T42
Files: app.json, modules/diorama-native/ios/, modules/diorama-native/src/flyoverCoverage.ts, modules/diorama-native/src/DioramaMapView.types.ts, OVERVIEW.md
Details: At WWDC26 Apple announced a Flyover upgrade for Apple Maps in iOS 27 that is widely reported to use Gaussian splatting for its 3D cities, rolling out city by city this fall. The owner wants Diorama on that latest MapKit, using whatever iOS 27 Apple Maps APIs give the highest-quality maps. Xcode 27.0 is now the only Xcode on the Mac and T42 makes iOS 27 SDK builds launch; the owner's iPhone runs iOS 27.0. Beyond splats, look for any iOS 27 MapKit API that raises quality: new map configurations or elevation styles, higher-resolution imagery or terrain, rendering, level-of-detail or anti-aliasing options, better tile loading so both eyes finish sharp together, and a way to ask MapKit whether a place has the new 3D (which could replace or back up the hand-kept coverage list). Adopt what visibly helps, each behind an availability check. Steps: (1) Research with the iOS 27 SDK docs and release notes whether third-party `MKMapView` gets the new rendering automatically, needs a new configuration/API (e.g. a new `MKMapConfiguration` option or elevation style), or is gated by the SDK the app links against; report what you find with links. (2) Adopt the new APIs behind `if #available(iOS 27, *)`, keeping the iOS 16.4+ fallback to `MKImageryMapConfiguration(elevationStyle: .realistic)`. (3) Re-check the things that depend on how MapKit draws: stereo alignment (T08 homography, vertical disparity ≈0 pt), the pitch cap and first-person camera (T23/T30/T31), the circle window and attribution (T26), the loading cover (T20), frame rate, and the coverage list (T28: splat cities may differ from the old Flyover list). (4) Update OVERVIEW.md's research table. If splats are not available to third-party apps yet, say so plainly and leave the app on the best available configuration.
Acceptance: The app builds with the iOS 27 SDK; the report states with sources whether MapKit exposes the splat rendering to apps and how the app uses it; on the owner's iOS 27 iPhone, a city Apple has upgraded renders with the new 3D where MapKit allows it (NEEDS DEVICE CHECK); stereo vertical disparity still ≈0 pt; typecheck, lint and tests pass.
Notes: Unblocked 2026-09-24: Xcode 27.0 (27A266a) is installed and its license accepted. The Simulator only has iOS 26.5 and 17.5 runtimes; get iOS 27 with `xcodebuild -downloadPlatform iOS`, or test on the owner's iPhone 14 Pro (iOS 27.0, UDID 00008120-001979A00EE8201E).

### T35 One-command Xcode archive pipeline for App Store distribution
Status: done
Depends: T14
Files: package.json, scripts/ios-prepare.sh, scripts/ios-archive.sh, scripts/bump-build.mjs, ios-export/ExportOptions.plist, app.json, docs/testflight.md, docs/app-store.md
Details: The owner wants the code building in Xcode so the app can go on the App Store. `ios/` is generated (Continuous Native Generation, git-ignored), and today the steps live only as prose in `docs/testflight.md`. Make it one command each, run from the repo root, never interactive: `npm run ios:prepare` (`expo prebuild -p ios --clean` + pods, then sanity checks: team `3U62R986E5`, bundle id, `CFBundleVersion` = `ios.buildNumber`, `ITSAppUsesNonExemptEncryption` = NO, icon compiled), `npm run ios:xcode` (prepare, then `xed ios` so the owner can Product → Archive by hand), `npm run ios:archive` (prepare, `xcodebuild archive` of the Release scheme for `generic/platform=iOS` with automatic signing, then `-exportArchive` with a committed `ios-export/ExportOptions.plist`, method `app-store-connect`, producing a signed `.ipa` under `ios/build/export/`; upload only when App Store Connect API key env vars are set: `ASC_KEY_ID`, `ASC_ISSUER_ID`, key file in `~/.appstoreconnect/private_keys/`), and `npm run bump` (increments `ios.buildNumber`; `--version x.y.z` for a new release). Release-build audit, fix what's wrong: no Expo dev launcher or dev menu in Release, `app/dev/*` redirects, the developer settings row hidden, no debug logging; a privacy manifest (`ios.privacyManifests` in app.json) that declares the required-reason APIs the app and its pods use (e.g. UserDefaults/MMKV, file timestamps, system boot time) so the upload doesn't get ITMS-91053; `PrivacyInfo.xcprivacy` present in the archive. Create and verify a signed App Store export locally (automatic signing with `-allowProvisioningUpdates` may create an Apple Distribution certificate in the owner's team; say if it does). Move the steps from `docs/testflight.md` into `docs/app-store.md` around the new commands.
Acceptance: `npm run ios:archive` (without API key env vars) produces a signed App Store `.ipa`; `codesign -dv` shows an Apple Distribution identity and `embedded.mobileprovision` is an App Store profile for `canvas23studios.diorama`; the archive's Info.plist has the right version, build and export-compliance keys; `PrivacyInfo.xcprivacy` is in the app; no dev-client UI in the Release app (launch it on the Simulator Release build and screenshot); `npm run bump` works; typecheck, lint and tests pass. The upload itself stays with T17 (needs the App Store Connect record and API key).
Notes: `npm run ios:prepare` / `ios:xcode` / `ios:archive` / `bump` work; `ios:archive` without ASC env vars produced a signed App Store .ipa (Apple Distribution: Evan Freymiller (3U62R986E5), Team Store profile for canvas23studios.diorama, get-task-allow false, beta-reports-active true, 1.0.0 (1), `ITSAppUsesNonExemptEncryption` false, icon, PrivacyInfo.xcprivacy, embedded JS bundle; no dev launcher/menu code). Bundle id is now `canvas23studios.diorama`. Signing reused the team's existing cloud-managed Apple Distribution cert; `-allowProvisioningUpdates` registered the App ID ("XC canvas23studios diorama", 9W737QGHUL) and an Xcode-managed App Store profile (R9J6S9LUQB). Privacy manifest: UserDefaults CA92.1, FileTimestamp C617.1/0A2A.1/3B52.1, SystemBootTime 35F9.1, DiskSpace E174.1/85F4.1, no tracking or collected data. Fixed a Release crash on `diorama://_sitemap` (`expo-router` `sitemap: false`). The Simulator's old com.ejf.diorama app was removed and `diorama://` approvals point at the new id. Docs: `docs/app-store.md`.

### T36 App Store listing: metadata, privacy answers, review notes, screenshots
Status: todo
Depends: T35
Files: store/, docs/app-store.md, scripts/screenshots.sh
Details: Prepare everything App Store Connect asks for, in the repo, so submitting is copy-paste: `store/metadata.md` (name "Diorama" plus a fallback name if taken, subtitle ≤30 chars, promotional text, description, keywords ≤100 chars, category suggestion, support URL and marketing URL placeholders, copyright), `store/privacy.md` (App Privacy answers: what the app collects; city searches go to Apple via MapKit; no tracking, no account; a short privacy policy text to host), `store/review-notes.md` (how a reviewer uses it with no headset: portrait full-screen mode, drag to look, the Settings toggles; the motion permission; Maps attribution), age rating answers, and export compliance (none). Screenshots: a script that boots the needed Simulators (6.9-inch iPhone 17 Pro Max required; add 6.5-inch only if App Store Connect still requires it), sets clean status bars (`xcrun simctl status_bar … override`), opens deep links (picker, preview, portrait viewer, landscape stereo, settings) in light mode, and saves PNGs at the exact required pixel sizes under `store/screenshots/`. Flag anything App Review may object to (T21 attribution under the blur, headset-only claims).
Acceptance: `store/` has metadata, privacy, review notes and a full set of correctly sized screenshots (check pixel sizes); `scripts/screenshots.sh` regenerates them; open questions for the owner listed at the top of `store/metadata.md`.

### T37 Pinch to zoom in portrait mode
Status: done
Depends: T31, T32, T35
Files: modules/diorama-native/ios/FirstPersonCamera.swift, modules/diorama-native/ios/DioramaMapView.swift, modules/diorama-native/ios/DioramaNativeModule.swift, modules/diorama-native/src/, src/screens/Viewer/ViewerGestures.tsx, src/features/viewer/, src/screens/Viewer/__tests__/
Details: The owner wants pinch to zoom in the portrait (full-screen, hand-held) Viewer. Zoom by moving the viewpoint, not by enlarging the picture: a pinch dollies the first-person vantage point along the current gaze toward (pinch out) or away from (pinch in) the aim point at the center of the view, like Apple Maps, so you zoom into what you are looking at. Keep the gaze direction; clamp the distance to the aim point to roughly 300–5000 m (the same bounds as T30's camera height) with a soft rubber-band at the ends; keep it smooth at 60 fps (native per-frame work, JS only sends the gesture: e.g. a ref method `setZoom(scale)` / `beginZoom`/`endZoom`, or a prop, whatever keeps the bridge quiet). Pinch works alongside the one-finger drag-to-look and head tracking (simultaneous gestures) and must not trigger the hold-to-exit or double-tap recenter. Recenter resets the look direction but keeps the zoom; leaving the Viewer or rotating to landscape resets the zoom to the place's normal distance. Portrait only: landscape stereo (headset) has no pinch. Re-check T31's pitch cap handling and T30's steepest-pitch start as the distance changes, and the MapKit pitch cap by altitude (the extra rotation must stay seamless).
Acceptance: In the Simulator portrait Viewer (option-drag pinch, or a temporary scripted zoom), pinching out moves toward the landmark at the view center and pinching in moves away, with no jump and the gaze direction unchanged (before/after screenshots at three zoom levels); the clamp holds at both ends; drag-to-look and double-tap recenter still work; landscape stereo ignores pinches; Jest covers the gesture wiring; typecheck, lint and tests pass. NEEDS DEVICE CHECK — pinch feels natural and smooth on the iPhone.
Notes: NEEDS DEVICE CHECK — the pinch feels natural at 60 fps; head tracking and the one-finger drag behave during and after a pinch; the spring-back at 300 m / 5 km and the ~3 km haze stop feel right; mono roll limit holds while zoomed; turning sideways resets the distance. A pinch dollies the vantage point along the gaze toward/away from the view-center ground point (gaze kept), rubber-banded at 300–5000 m (0.55 stretch, ≤25% past, 25-frame spring back). New native ref methods `beginZoom`/`setZoom(scale)`/`endZoom`/`resetZoom`. Gestures: Race(Simultaneous(drag, pinch), Exclusive(doubleTap, tap, hold)); VoiceOver "Zoom in/out" actions. Extra stop: pinching out halts (rubber-banded) where MapKit last drew the view center (~3 km for Paris) so the view never fills with haze (one `if` in `setZoom` to allow it). Near/above the horizon the dolly runs along a line 15° below the horizon. Recenter keeps the zoom; rotating to landscape or leaving resets it; landscape ignores pinch.

### T39 "Current location" row at the top of the picker
Status: done
Depends: T10, T25, T35
Files: package.json, package-lock.json, app.json, jest.setup.ts, src/features/location/**, src/screens/CityPicker/CurrentLocationSection.tsx, src/screens/CityPicker/CityPickerScreen.tsx, src/screens/CityPicker/__tests__/, store/privacy.md
Details: The owner wants to open the diorama where they are standing. Add a one-row inset-grouped section above Recent (only while the search is empty): "Current location" with the SF Symbol `location.fill` in systemBlue, like Apple Maps. Not swipe-deletable. Install `expo-location` with `npx expo install expo-location` (the only new dependency) and add its config plugin in app.json with `locationWhenInUsePermission` (sentence case, e.g. "Diorama uses your location to show the city around you."). **When-in-use only**: `isIosBackgroundLocationEnabled: false`, no `NSLocationAlwaysAndWhenInUseUsageDescription`, no `location` background mode. Ask for permission only when the row is tapped, never at launch (`requestForegroundPermissionsAsync`). Put the logic in a TanStack Query mutation hook `useLocateMe()` in `src/features/location/`: permission → `getCurrentPositionAsync` (Balanced accuracy, ~10 s timeout) → `reverseGeocodeAsync` for a name (the street or place name; fall back to "Current location") and a "Town, Country" subtitle → a `RecentCity` with a `placeId(name, lat, lon)` id and the address altitude (`PLACE_ALTITUDES.address`) → `useShowCity()`, so it lands in Recent and reopens offline like any place. Row states: while locating, a spinner or "Locating…" subtitle, repeat taps ignored; denied or restricted → subtitle "Location access is off" and a tap opens iOS Settings (`Linking.openSettings()`); Location Services off, timeout or no fix → a quiet "Can't find your location" subtitle, tap to retry. Leaving the picker mid-locate cancels the push (like T10's resolve). Mock expo-location in Jest. If `store/privacy.md` exists (T36), add location: used on the device to center the map, not collected or linked to the user.
Acceptance: Jest (expo-location mocked): granted → the preview route opens on the located place and it's in Recent; denied → "Location access is off" and a tap calls `Linking.openSettings`; a failed fix shows the retry message; the section is hidden while searching. After `npx expo prebuild -p ios`, the generated Info.plist has `NSLocationWhenInUseUsageDescription` and no Always key or `location` background mode. Simulator: `xcrun simctl location booted set 42.3601,-71.0589`, `xcrun simctl privacy booted grant location canvas23studios.diorama`, tap the row → the preview is framed on that spot; screenshots of the picker in light and dark. typecheck, lint and tests pass. NEEDS DEVICE CHECK — the permission prompt and its text, a real GPS fix.
Notes: NEEDS DEVICE CHECK — the permission prompt and its text; a real GPS fix indoors and outdoors; a tap with access off opens Diorama's own Settings page (the Simulator opened the main one); with Precise Location off the place is named after the town; rebuild the installed dev client (the old binary has no ExpoLocation module and crashes on the picker). `useLocateMe()` = a query that reads access without prompting (on mount and on each return to the app, so "Location access is off" shows from launch and clears after Settings) + a mutation: prompt only when undetermined → Balanced fix (10 s timeout) → reverse geocode (5 s, falls back to "Current location") → `useShowCity`. The services/access check is also time-limited (it hung after a Simulator reboot). A fix worse than 1 km is named after the town. For T36: location is used on the device only; the coordinates go to Apple for naming. `store/privacy.md` didn't exist yet, so it was skipped.

### T40 Follow my location live (real-time GPS)
Status: done
Depends: T37, T39
Files: modules/diorama-native/ios/DioramaMapView.swift, modules/diorama-native/ios/DioramaNativeModule.swift, modules/diorama-native/ios/FirstPersonCamera.swift, modules/diorama-native/ios/StereoRig.swift, modules/diorama-native/src/, src/features/location/**, src/screens/CityPicker/CurrentLocationSection.tsx, src/screens/CityPreview/**, src/screens/Viewer/ViewerMap.tsx, src/features/viewer/, OVERVIEW.md
Details: The owner wants the diorama to track their GPS position in real time: as they walk or ride, the model city moves with them. Opening from T39's "Current location" row starts live mode: pass a `live=1` search param to `/city/[cityId]` and on through "Enter Diorama" to `/view/[cityId]` (reopening that place from Recent is a fixed place, no param). In live mode a hook `useLiveLocation()` in `src/features/location/` runs `watchPositionAsync` (High accuracy, `distanceInterval` ~5 m), drops fixes with horizontal accuracy worse than ~50 m, and stops when the screen loses focus or the app goes inactive (reuse `useAppIsActive`). Foreground only, when-in-use permission only (T39 already asked); if access was revoked, stay on the fixed place quietly. Native: changing the `center` prop today counts as a new place (new vantage point, onReady again, loading cover, head recenter), so add a ref method (e.g. `followTo(latitude, longitude)`) that glides the center and vantage point to the new spot over ~1 s with a critically damped spring on the display link, keeping head look, pinch zoom (T37), orbit angle, eye separation and T30/T31 pitch-cap handling as the ground height changes, both eyes updated in the same frame; under Reduce Motion use a short fade or a very quick glide. JS sends at most ~1 location per second; Swift does all smoothing. While live, show Apple's blue location dot (`MKMapView.showsUserLocation`) in every eye so it sits at the right depth in stereo; check it stays readable under the miniature blur. The preview card reads "Following your location" with a `location.fill` glyph while live. No compass heading: head tracking already turns the view. Update OVERVIEW.md's ref-method contract.
Acceptance: Simulator: open live mode, then `xcrun simctl location booted start --speed=10 42.3601,-71.0589 42.3555,-71.0640` while the preview and the portrait Viewer are open: the city slides smoothly along the route with no loading cover, no jump in look direction, zoom kept, blue dot visible (screenshots at three points on the route); leaving the screen or backgrounding stops the watch (Jest); a Recent reopen is fixed; Jest covers the `live` param wiring and the accuracy filter with expo-location mocked; typecheck, lint and tests pass. NEEDS DEVICE CHECK — walk a block in portrait and in stereo: motion smooth, nothing sudden or queasy, battery and thermals over 10 minutes (see T15).
Notes: NEEDS DEVICE CHECK — walk a block in portrait and stereo (smooth, nothing sudden); battery and thermals over 10 min (High-accuracy watch + MapKit's own updates for the dot); in stereo the blue dot sits at the right depth and its pulsing halo stays in step between eyes; stereo live mode and Reduce Motion (0.25 s spring per fix) not tried in the Simulator; MapKit draws the dot on rooftops over 3D buildings. Native: ref method `followTo(lat, lon)` + prop `showsUserLocation`; the glide is an offset from the `center` prop moving model center, vantage point and zoom together (a new `center` drops it; snaps while loading/off screen). A plain 1 s spring stuttered (move 0.6 s, stop 0.45 s), so the target slides fix-to-fix over the time between fixes (≤4 s) and a critically damped ~1 s spring rounds starts/stops/turns. JS: `useLiveLocation` (50 m filter, ≤1 fix/s, stops out of focus/inactive) + `useFollowMyLocation(mapRef)`; `live=1` flows picker → preview → Viewer → back. StereoRig re-asks MapKit's pitch cap only after the center moves 10% of the camera distance.

### T41 National parks section in the picker (Grand Canyon, Yellowstone, …)
Status: done
Depends: T06, T28, T33
Files: src/features/cities/curated.ts, src/features/cities/__tests__/, src/screens/CityPicker/ParksSection.tsx, src/screens/CityPicker/CityPickerScreen.tsx, src/screens/CityPicker/curatedMatch.ts, src/screens/CityPicker/__tests__/
Details: The owner wants US national parks as ready-made places, starting with Grand Canyon and Yellowstone. Add about 10 parks to the curated data with a `category: 'city' | 'park'` field (so `getCuratedCity`/`useCity` keep working unchanged): Grand Canyon (South Rim), Yellowstone (Grand Prismatic Spring or the Grand Canyon of the Yellowstone, whichever frames better), Yosemite Valley, Zion Canyon, Grand Teton, Arches, Bryce Canyon, Glacier, Mount Rainier, Rocky Mountain; swap any that look poor. Subtitle "State, United States"; nature SF Symbols (`mountain.2.fill`, `tree.fill`, …) and fitting tile colors. There are no Flyover buildings here; the draw is MapKit's realistic-elevation terrain, so pick a center, altitude, pitch and heading per park by screenshot (`diorama://dev/map?lat=…&lon=…&altitude=…&pitch=…&heading=…`); expect larger altitudes than cities (roughly 2–5 km) and remember MapKit's pitch cap drops with distance. Parks must come out `unknown` coverage (T28), so no "no 3D buildings" note; add a test for that. Picker: Featured keeps cities only; a new inset-grouped "National parks" section sits below it, rows swipe-to-delete into the same hidden store as Featured (T33), so "Restore suggested places" brings them back too; hide the header when all are hidden. Extend `curatedMatch` so a search result like "Grand Canyon" near a curated park opens the tuned entry. Check first person over deep relief: MapKit stands the camera on the ground height under its look-at point (OVERVIEW known limit, ~1% bob over cities), and across a 1.8 km-deep canyon that bob may be large; measure it on the dev map with debugLook, and if it's bad pick framings that keep it small and note it.
Acceptance: Jest: ids unique across cities and parks, coordinates in range, every park is `unknown` coverage, the picker shows a "National parks" section below Featured, deleting a park hides it and Restore brings it back, a "Grand Canyon" search result opens the curated entry. Simulator screenshots: the picker section (light and dark), and the preview of Grand Canyon and Yellowstone showing real 3D terrain. typecheck, lint and tests pass. NEEDS DEVICE CHECK — stereo over the Grand Canyon reads as a tabletop model and the vantage point doesn't bob uncomfortably when looking across the canyon.
Notes: NEEDS DEVICE CHECK — stereo over the Grand Canyon reads as a tabletop model and a head turn across it doesn't bob uncomfortably (also try Grand Teton and Yosemite, the worst estimates); whether the gray unloaded grid at the top of the preview (mostly Grand Teton at some orbit angles) shows on a real iPhone. Ten parks in `CURATED_PARKS` (`category: 'park'`), found by `getCuratedCity`/`useCity`, sharing Featured's hidden list; framings 2.5–4.5 km at 52–55° (≥55° leaves a gray grid); Arches uses the Fiery Furnace. `curatedMatch` now decides by kind and parks match within 120 km (Apple's "Grand Canyon" point is ~95 km west of the South Rim); `useOpenSearchResult` always calls it; "No places" shows only when parks are hidden too. Bob estimated from elevation data (no scripted drag in the Simulator), ±60° stereo turns: Grand Canyon 0 to -18% (centered on the South Rim), Yellowstone ±5%, summit views sink 22–41% (Grand Teton worst). Follow-ups: aim at where the gaze meets the terrain (small per-park elevation grid); Settings' Restore footer still says "featured cities".

### T42 Adopt the UIScene life cycle so iOS 27 SDK builds launch
Status: in-progress
Depends: T35
Files: plugins/withSceneLifecycle.js, app.json, modules/diorama-native/ios/, src/features/viewer/, docs/app-store.md
Details: Xcode auto-updated to 27.0 (the only Xcode on the Mac), and a device build now crashes at launch on the owner's iOS 27 iPhone before any JS loads: `EXC_BREAKPOINT` in `_UIApplicationEvaluateRuntimeIssueForNoSceneLifecycleAdoption` (crash log `Diorama-2026-09-24-203730.ips`). iOS 27 refuses to launch apps linked against its SDK that still use the app-delegate window. The prebuilt `ios/Diorama/AppDelegate.swift` creates `UIWindow(frame:)` and calls `startReactNative(in:)` in `didFinishLaunching`, and Info.plist has no `UIApplicationSceneManifest`; the latest Expo 57 template (expo-template-bare-minimum 57.0.27) still doesn't wire scenes. Expo 57.0.24 ships `ExpoAppSceneDelegate` (`node_modules/expo/ios/AppDelegates/ExpoAppSceneDelegate.swift`, objc name `EXExpoAppSceneDelegate`; read its doc comment: the app delegate must subclass `ExpoAppDelegate`, conform to `ExpoReactNativeFactoryProvider` and create the factory in `didFinishLaunching`). `ios/` is generated (CNG), so write a local config plugin `plugins/withSceneLifecycle.js`, registered in app.json, that adds the scene manifest (single scene, `UIWindowSceneSessionRoleApplication` → Expo's scene delegate) and patches AppDelegate to the provider pattern (no window or `startReactNative` there). Then audit everything that assumed the old life cycle: our Swift (`UIApplication.shared.windows`/`keyWindow`, `statusBarOrientation`, interface orientation used for the landscape head-tracking frame, `UIScreen.main`), portrait lock and the landscape-only Viewer (react-native-screens / expo-screen-orientation), cold-start deep links (`diorama://city/paris` when the app isn't running), the dev-client launcher, splash screen, status bar hiding in the Viewer, and keep-awake. Must still work on iOS 16.4+ devices and the iOS 26.5 Simulator. The owner's iPhone 14 Pro (iOS 27.0, UDID 00008120-001979A00EE8201E) is connected and unlocked: build with `npx expo run:ios --device 00008120-001979A00EE8201E`, launch with `xcrun devicectl device process launch`, and read new crash logs with `xcrun devicectl device info files --domain-type systemCrashLogs`. Note the new signing/Xcode facts in `docs/app-store.md`.
Acceptance: `npx expo prebuild -p ios --clean` output contains the scene manifest and patched AppDelegate; the dev client built with Xcode 27 launches on the owner's iOS 27 iPhone, loads JS from Metro and shows the picker with the "Current location" row, with no new Diorama crash log; the iOS 26.5 Simulator build still launches; a cold-start deep link opens its route; the Viewer still rotates to landscape and the picker stays portrait; `npm run ios:archive` still produces a signed .ipa; typecheck, lint and tests pass. NEEDS DEVICE CHECK — head tracking frame in landscape, orientation switching, and exit from the Viewer on iOS 27.

### T43 Pick a place by moving a map ("Choose on map")
Status: todo
Depends: T39, T42
Files: modules/diorama-native/ios/PlacePickerMapView.swift, modules/diorama-native/ios/DioramaNativeModule.swift, modules/diorama-native/src/PlacePickerMapView.tsx, modules/diorama-native/src/PlacePickerMapView.types.ts, modules/diorama-native/src/index.ts, app/pick.tsx, app/_layout.tsx, src/screens/PlacePicker/**, src/screens/CityPicker/CurrentLocationSection.tsx, src/screens/CityPicker/__tests__/, src/features/location/placeHere.ts
Details: The owner wants to pick any spot by moving a map, not only by search or GPS. Add a second row under "Current location" in the picker's top section: "Choose on map" with the SF Symbol `mappin.and.ellipse`. It opens a new route `app/pick.tsx` → `src/screens/PlacePicker/`, presented as a native sheet (large detent, grabber, swipe down to cancel). The sheet is a full-bleed interactive Apple map with a fixed pin at the center, like Apple Maps' "Move pin to location": pan and pinch move the map under the pin; a tap moves the map so that point is centered. Native side, keep it small: a new `PlacePickerMapView` in the Swift module wrapping one `MKMapView` (`MKHybridMapConfiguration`, flat, north up, no pitch or rotate, POIs on) with props for the starting center and span and one event `onRegionChangeEnd({ latitude, longitude, spanMeters })`; it shows the blue location dot only if location access is already granted, and never asks for it. Starting view: the user's location if access is granted, else the newest Recent place, else New York. A glass card at the bottom names the spot under the pin (reuse T39's reverse-geocode naming in `placeHere.ts`, debounced, stale answers dropped; fall back to coordinates like "40.7484° N, 73.9857° W") with the T28 coverage state ("3D buildings" for `yes`, T11's terrain note for `no`, nothing for `unknown`), and a capsule "Open diorama" button. Opening builds a `RecentCity` (`placeId(name, lat, lon)` id; altitude from the visible span, about width / 12, clamped 600–4500 m) and uses `useShowCity()`, so it lands in Recent and the preview opens (dismiss the sheet first). The Simulator can't drag a map from a script, so the route also reads optional `lat`, `lon` and `span` search params (`diorama://pick?lat=48.8584&lon=2.2945&span=2000`). Light and dark, Dynamic Type, VoiceOver ("Map. Drag to move the pin." and a labelled Open button), haptic on open.
Acceptance: Jest (native view and reverse geocoding mocked): the row opens `/pick`; a region-change event updates the name and coverage in the card; "Open diorama" adds the place to Recent with the span-based altitude and opens its preview; a stale geocode answer never replaces a newer one. Simulator: `diorama://pick?lat=48.8584&lon=2.2945&span=2000` shows the map centered on the Eiffel Tower with the pin and card (screenshots in light and dark), and Open lands on the Paris preview framed there. typecheck, lint and tests pass. NEEDS DEVICE CHECK — pan, pinch and tap feel like Apple Maps; the sheet's swipe-down doesn't fight map panning.

### T44 Apple Maps logo and "Legal" show double in the two-eye view
Status: todo
Depends: T26, T31
Files: modules/diorama-native/ios/EyeView.swift, modules/diorama-native/ios/StereoRig.swift, modules/diorama-native/ios/StereoGeometry.swift, modules/diorama-native/src/__tests__/
Details: On the owner's headset (3D-printed, lenses exactly 64 mm apart = the default Lens spacing) the city fuses but MapKit's Apple Maps logo and "Legal" text are seen double. Likely cause: the attribution is drawn inside each eye's `MKMapView` (placed through layout margins in `EyeView.swift`), so it rides that eye's stereo warp (T08 toe-in homography) and zero-parallax slide. Those differ between the eyes on purpose (they give the city its depth), which leaves the flat text at different spots relative to each circle, possibly with vertical disparity too, so it can't fuse. Measure first: in a landscape stereo Simulator screenshot, find the logo and Legal in each eye relative to that circle's center, at rest and while looking around with debugLook (including T31's lift past MapKit's cap). Then place the attribution so both copies sit at the same spot relative to their circle centers (within about 1 pt), so they fuse at the depth of the circle rim like a label on the window. For example, pre-compensate each eye's layout margins for its own warp, or counter-transform the attribution, whichever MapKit allows; keep MapKit's own logo and Legal link (no custom copy) and keep both visible, since Apple requires attribution. Leave T21's blur question alone. Check that full-screen mono is unchanged.
Acceptance: Landscape stereo Simulator screenshots at rest, looking left/right/up/down with debugLook, and past the cap: in each, the logo and Legal sit at the same position relative to their circle centers in both eyes (measured offsets ≤1 pt horizontally and vertically, listed in Notes); the city's stereo is unchanged (vertical disparity ≈0 pt); mono is unchanged; typecheck, lint and tests pass. NEEDS DEVICE CHECK — in the owner's headset, the logo and Legal read as single, crisp text.

### T45 Settings preview always shows downtown Boston
Status: todo
Depends: T13, T41
Files: src/screens/Settings/useSampleCity.ts, src/screens/Settings/SettingsPreviewMap.tsx, src/screens/Settings/__tests__/
Details: The owner wants the example map in Settings to always be downtown Boston. Today `useSampleCity()` shows the last featured place you opened (a park since T41) and falls back to the first featured city (New York). Make it always the curated `boston` entry, found with `getCuratedCity('boston')` so it still works when Boston is hidden from Featured (T33). Its center (42.3565, -71.061) is already downtown; check the preview frames the Financial District and Government Center skyline well at the preview's size. If it doesn't, add a Settings-only framing (center, altitude, pitch, heading) next to the hook rather than changing the Featured Boston entry. Update the hook's doc comment and the Settings tests that assume the old choice.
Acceptance: Jest: the Settings preview uses downtown Boston whether Recent is empty, starts with a park or a searched place, or Boston is hidden from Featured. Simulator: `diorama://settings` screenshot shows downtown Boston's skyline in the preview (light and dark). typecheck, lint and tests pass.

## Backlog (not scheduled)

### T19 Picker large title collapses after exiting a deep-linked Viewer
Status: todo
Depends: T12
Files: src/screens/CityPicker/**, app/_layout.tsx
Details: After opening `diorama://view/<id>` directly and holding to exit, then going back to the picker, its large title is collapsed (scrolling restores it). Cause: the picker is laid out in landscape under the modal, where iPhone hides large titles, and its scroll offset stays at the small-bar position. expo-router renders the picker as focused before pushing the linked route, so JS can't reliably detect the case. Likely needs a tiny native scroll-to-top (react-native-screens) or a react-native-screens fix. Cosmetic and deep-link only; the normal flow is fine.
Acceptance: After the deep-link flow above, the picker shows its expanded large title (Simulator screenshot).

### T38 Friendly fallback for unknown deep links
Status: todo
Depends: T35
Files: app/+not-found.tsx, src/screens/__tests__/
Details: A bad or stale deep link (e.g. `diorama://city/…` for a removed id, or any unknown path) shows expo-router's generic "Unmatched Route" screen. Add `app/+not-found.tsx` that quietly redirects to `/` (the picker), or shows a short sentence-case message with a button back, matching the app's style.
Acceptance: Jest: an unknown path renders the fallback / lands on `/`; typecheck, lint and tests pass.
